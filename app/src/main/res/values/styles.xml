<resources>

    <style name="Widget.Theme.DarFit.ButtonBar.Fullscreen" parent="">
        <item name="android:background">@color/black_overlay</item>
        <item name="android:buttonBarStyle">?android:attr/buttonBarStyle</item>
    </style>

    <style name="CustomBottomNavigationView" parent="Widget.MaterialComponents.BottomNavigationView">
        <item name="itemRippleColor">@null</item> <!-- Убирает эффект нажатия -->
        <item name="itemBackground">@android:color/transparent</item> <!-- Убирает заливку фона -->
    </style>

</resources>